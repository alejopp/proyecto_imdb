<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alejobootcampandroid">

    <uses-permission android:name="android.permission.INTERNET" />


    <!-- <manifest> y <application>. Deben aparecer una sola vez -->
    <!-- <application> debe ser el último dentro del elemento <manifest> -->
    <!--<uses-feature android:name="android.hardware.sensor.compass" -->

    <!--Contiene atributos que afectan a los demás componentes-->
    <application
        android:name=".AlejoBootcampAndroidApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticeGroupApplication">

        <activity
            android:name="com.example.alejobootcampandroid.presentation.ui.MainActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>